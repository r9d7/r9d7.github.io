---
import dayjs from "dayjs";
import utc from "dayjs/plugin/utc";

dayjs.extend(utc);

import BaseLayout from "~/layouts/BaseLayout.astro";

const allPosts = await Astro.glob("./content/writing/*.md");
---

<BaseLayout pageTitle="Home">
  <ul class="space-y-3">
    {
      allPosts
        .sort((a, b) =>
          dayjs(a.frontmatter.pubDate).isAfter(dayjs(b.frontmatter.pubDate))
            ? -1
            : 1,
        )
        .map((post) => (
          <li>
            <d class="inline-flex w-24 text-muted-foreground">
              {dayjs(post.frontmatter.pubDate).format("YYYY • MM")}
            </d>
            <a href={post.url}>{post.frontmatter.title}</a>
          </li>
        ))
    }
  </ul>
</BaseLayout>
